# ─────────────────────────────────────────────────────────────────────────────
# PWIS Continuous Integration
# Runs on every push and pull request to main/dev branches.
#
# Jobs:
#   lint     — ruff + black check (fast, <30s)
#   test     — pytest with coverage gate (70% minimum)
#   data-quality — validate synthetic data schema and value ranges
#   build-check  — verify all scripts execute without error
# ─────────────────────────────────────────────────────────────────────────────

name: PWIS CI

on:
  push:
    branches: [main, dev, "feature/**"]
  pull_request:
    branches: [main, dev]

env:
  PYTHON_VERSION: "3.11"

jobs:
  # ── 1. LINT ───────────────────────────────────────────────────────────────
  lint:
    name: Lint & Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install lint tools
        run: pip install ruff black

      - name: Run ruff (linting)
        run: ruff check models/ gis/ tests/

      - name: Run black (formatting check)
        run: black --check --diff models/ gis/ tests/

  # ── 2. TEST ───────────────────────────────────────────────────────────────
  test:
    name: Test Suite + Coverage
    runs-on: ubuntu-latest
    needs: lint
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run pytest with coverage
        run: |
          pytest tests/ \
            --cov=models \
            --cov-report=xml \
            --cov-report=term-missing \
            --tb=short \
            -v

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        if: matrix.python-version == '3.11'
        with:
          files: coverage.xml
          flags: unittests
          name: pwis-coverage
        continue-on-error: true   # Don't fail build if Codecov is unavailable

  # ── 3. DATA QUALITY ───────────────────────────────────────────────────────
  data-quality:
    name: Data Schema Validation
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install dependencies
        run: pip install pandas numpy

      - name: Generate synthetic data
        run: python data/generate_data.py

      - name: Validate data schemas
        run: |
          python - << 'PYEOF'
          import pandas as pd
          import sys

          errors = []

          # pipe_segments
          df = pd.read_csv("data/pipe_segments.csv")
          if not (df["condition_score"].between(1, 100)).all():
              errors.append("pipe_segments: condition_score out of range [1,100]")
          if df["segment_id"].duplicated().any():
              errors.append("pipe_segments: duplicate segment_ids found")
          if not df["segment_id"].notna().all():
              errors.append("pipe_segments: null segment_ids found")
          required_cols = ["segment_id","system_type","corridor_name","district",
                           "pipe_material","condition_score","lat","lon"]
          missing = [c for c in required_cols if c not in df.columns]
          if missing:
              errors.append(f"pipe_segments: missing columns {missing}")
          valid_systems = {"Water", "Sewer", "Stormwater"}
          bad_sys = set(df["system_type"].unique()) - valid_systems
          if bad_sys:
              errors.append(f"pipe_segments: unexpected system_type values {bad_sys}")

          # work_orders
          df = pd.read_csv("data/work_orders.csv")
          if df["work_order_id"].duplicated().any():
              errors.append("work_orders: duplicate work_order_ids found")

          # service_requests
          df = pd.read_csv("data/service_requests.csv")
          valid_severity = {"Low", "Medium", "High", "Critical"}
          bad = set(df["severity"].unique()) - valid_severity
          if bad:
              errors.append(f"service_requests: unexpected severity values {bad}")

          # budget_cip
          df = pd.read_csv("data/budget_cip.csv")
          pct_sum = df["water_budget_pct"] + df["sewer_budget_pct"] + df["stormwater_budget_pct"]
          if (pct_sum > 102).any():
              errors.append("budget_cip: system pct columns sum > 100 in some rows")

          if errors:
              print("DATA QUALITY FAILURES:")
              for e in errors:
                  print(f"  ✗ {e}")
              sys.exit(1)
          else:
              print("✓ All data quality checks passed")
          PYEOF

  # ── 4. BUILD CHECK ────────────────────────────────────────────────────────
  build-check:
    name: Smoke Test — Scripts Run End-to-End
    runs-on: ubuntu-latest
    needs: [test, data-quality]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -e ".[dev]"
          pip install folium

      - name: Generate data
        run: python data/generate_data.py

      - name: Run prioritization model
        run: python models/prioritization.py

      - name: Run scenario engine
        run: python models/scenario_engine.py

      - name: Generate GIS maps
        run: python gis/map.py

      - name: Verify all outputs exist
        run: |
          python - << 'PYEOF'
          from pathlib import Path
          required = [
              "data/priority_scores.csv",
              "data/deferral_analysis.csv",
              "docs/map_condition.html",
              "docs/map_priority.html",
              "docs/map_service_requests.html",
              "docs/map_executive.html",
          ]
          missing = [f for f in required if not Path(f).exists()]
          if missing:
              print(f"MISSING OUTPUTS: {missing}")
              import sys; sys.exit(1)
          print(f"✓ All {len(required)} expected outputs present")
          PYEOF

  # ── 5. NOTIFY (main branch only) ──────────────────────────────────────────
  notify:
    name: Deployment Gate
    runs-on: ubuntu-latest
    needs: build-check
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Confirm PROD promotion readiness
        run: |
          echo "All CI gates passed on main branch."
          echo "PWIS is ready for PROD deployment."
          echo "Next step: Analytics Lead to trigger pbi-tools deployment."
